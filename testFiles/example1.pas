program example(input, output);
	var x, y :  integer; (*
	Temp temp temp *)
	//asdlkansd'l a;s dak
	var temp :  array [ 3 .. 5 ] of integer ;
	var temp2 : array [ 4 .. 5 ] of integer ;
	function gcd(a, b: integer): integer;
	begin
	   if b = 0 then gcd := a
	   else gcd := gcd(b, a mod b)
	end;
	procedure other(thing : array [ 3 .. 5 ] of integer );
	begin
		thing[0] := 1
	end;

begin
	while 0 do begin end;
	for x := 1 to 5 do begin end;
	// read;
	other(temp);
	temp[3]:=a;
	read(x);
	write(x[0]);
	write(gcd(x, y))
end.
